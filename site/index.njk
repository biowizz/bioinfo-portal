---
layout: base.njk
title: "Bioinformatics Conferences & Jobs"
permalink: /index.html
---
{% import "listing-card.njk" as cards %}
{% import "filter-bar.njk" as filters %}

<h1>All listings</h1>
<p class="lede">Bioinformatics conferences and jobs, curated by hand.</p>

{{ filters.filterBar(true) }}

<div class="card-grid" data-card-grid>
  {% for item in listings %}
  {{ cards.card(item) }}
  {% endfor %}
</div>

<p class="empty-state" data-empty-state hidden>No listings match those filters.</p>
